import tw from 'twin.macro';
import { PageWrapper, DemoWrapper } from '@/components';

export default function TailwinduiPage() {
  return (
    <PageWrapper>
      <DemoWrapper />
    </PageWrapper>
  );
}
